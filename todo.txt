Redo streamlined: Reduced production cost. Further from base is not possible because of stealth rules.

repairAction -> start repair and finish repair on the next active turn. onHit -> remove IS_REPAIRING flag.

TweenManager for effects.

air and submerged do not get hit by pierce
engineer unit. needs table with units it can create.
that table is based on map & availability.

engineerUnits -> CATEGORY -> type[]

shopDataSheet: shop only has 1 array of entity types. i get the category via inference