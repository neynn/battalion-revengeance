repairAction -> start repair and finish repair on the next active turn. onHit -> remove IS_REPAIRING flag.

Model for Destroyer
Data for Destroyer

train as transport.

MapEditor:
    1. Add Text -> Runtime HashSet that gets turned into an array when creating the map.
    2. Add Custom -> Runtime HashSet that gets turned into an array when creating the map.

Missing Terrain:
    Reduced Mobility: Ship movement is reduced.
    Not Selectable: Trait that disallows selection of units.

Building traits:
    Proficiency traits: Each one gives its respective type -5% cost!

Unit traits:
    Landbound, Airbound?

BUG:
  With a selected entity -> clicking outside the game keeps it selected. Remove the state!