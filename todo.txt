repairAction -> start repair and finish repair on the next active turn. onHit -> remove IS_REPAIRING flag.

Model for Destroyer
Data for Destroyer

train as transport.

MapEditor:
    1. Add Text -> Runtime HashSet that gets turned into an array when creating the map.
    2. Add Custom -> Runtime HashSet that gets turned into an array when creating the map.

Missing Terrain:
    Reduced Mobility: Ship movement is reduced.

Building traits:
    Proficiency traits: Each one gives its respective type -5% cost!

Unit traits:
    Landbound, Airbound?

BUG:
  With a selected entity -> clicking outside the game keeps it selected. Remove the state!

Rendering:
    Tile by tile -> no more layers.
    This adds depth to entities and their map!

Z-Buffer:
    
transportable terrain -> allows to use the sea transport

metasprite: directions as table. all else use start.

Misconception: Entities do NOT get deleted, they are kept in memory until the match ends.
EntityManager keeps them ALL until the simulation is terminated.
DestroyEntity does not really destroy it.

Replicated authoritative state driven by command intents

Missions contain data like units and spawns.
Move stuff like dialogue ect OUT of the map.
Problem: ONE global "text" array for maps. This is SHIT. Separate them!
entity custom ids should be mission defined!
map.json is JUST world template.
separate map from mission

Static World Data:
    Music
    Data
    Buildings
    Flags
    Localization
Scenario:
    Client
    Teams
    Entities
    BuildingOwnership
    Mines
    Objectives
    Prelogue
    Postlogue
    Defeat
    Events
    Playlist


Mode is external
Instead of eventName give each event an id. When loading check if they are valid.
save/load does NOT care about the split in scenario, it does however save missionID and the general mission tree.

TODO on the server: selectSCENARIO, not just map.
Remove playlist from mission

CreateObjective NEEDS parsed data.
MapPreview -> remove TEXT

if building is unclaimed -> use unclaimed sprite

buildings do not have their own color.
if a building is unclaimed it has the unclaimed sprite that is always the same,
if a building is claimed it has the teams color
the teams color is determined when the scenario is loaded
loadAsFaction in its current form does not work it has to collapse the color into one value that is read when the team is loaded intor the runtime